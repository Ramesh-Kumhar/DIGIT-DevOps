repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami
  
releases:
- name: application
  chart: ./backend/application
  namespace: egov
  installed: true
  version: 0.1.0
  values:
    - ./backend/application/values.yaml
